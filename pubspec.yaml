name: wt_console_designer
description: A new Flutter project.
version: 1.0.0+1
homepage: https://github.com/tmcmaster/wt_console_designer
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences:
  flutter_riverpod:
  uuid:
  flutter_colorpicker: ^1.0.3
  font_awesome_flutter:
  lottie: ^2.1.0
  dart_mappable: ^1.2.0
  freezed_annotation: ^2.2.0
  json_annotation: ^4.7.0

  wt_firepod:
    git:
      url: git@github.com:tmcmaster/wt_firepod.git
  wt_logging:
    git:
      url: git@github.com:tmcmaster/wt_logging.git
  wt_app_scaffold:
    git:
      url: https://github.com/tmcmaster/wt_app_scaffold.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  dart_mappable_builder: ^1.2.1
  freezed: ^2.2.0
  json_serializable: ^6.5.4

flutter:
  uses-material-design: true

  assets:
    - assets/